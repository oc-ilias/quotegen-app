/**
 * QuoteGen Type Definitions (Backward Compatibility)
 * 
 * This file re-exports all types from the centralized types/index.ts
 * for backward compatibility with existing imports.
 * 
 * @deprecated Import from @/types instead for new code
 * @module types/quote
 */

// Re-export everything from the centralized types
export * from './index';

// Default export for convenience
export { default } from './index';
